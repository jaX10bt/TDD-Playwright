templates: 
  - name: label_switch
  - components: 
      parent: switch
      children: Label + Switch
      - name: Label
        type: Label
        props:
          label: " {{ labelSwitch }}"
          
      - name: Switch
        type: Switch
        props:
          switch: "button.switch__button"